# MCP Server Configuration
# Copy this file to .env and fill in your values

# Remote Azure Function App URL (from azd env get-values)
MCP_REMOTE_URL=https://your-function-app.azurewebsites.net/runtime/webhooks/mcp

# MCP Extension Key (from az functionapp keys list)
MCP_REMOTE_KEY=your-mcp-key-here

# Local development (no changes needed)
MCP_LOCAL_URL=http://localhost:7071/runtime/webhooks/mcp
